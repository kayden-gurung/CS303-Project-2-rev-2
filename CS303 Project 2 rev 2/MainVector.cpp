#include <iostream>
#include "Vector.h"

using namespace std;
/*
int main() {
    Stack<int> myStack;  // Create a stack of integers

    // 1. Check if the stack is empty
    cout << "Is the stack empty? " << (myStack.empty() ? "Yes" : "No") << endl;

    // 2. Insert some values onto the stack
    myStack.push(10);
    myStack.push(20);
    myStack.push(30);
    myStack.push(40);

    // 3. Check if the stack is empty after pushing values
    cout << "Is the stack empty after pushing elements? " << (myStack.empty() ? "Yes" : "No") << endl;

    // 4. Find the top element of the stack
    cout << "Top element is: " << myStack.top() << endl;

    // 5. Remove an element from the stack
    cout << "Popped element: " << myStack.pop() << endl;

    // 6. Find the new top element
    cout << "New top element is: " << myStack.top() << endl;

    // 7. Calculate the average of the stack elements
    cout << "Average value of the stack elements: " << myStack.average() << endl;

    return 0;
}
*/